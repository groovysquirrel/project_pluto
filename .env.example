# ==============================================================================
# PROJECT PLUTO - Environment Configuration Example
# ==============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# DOMAIN CONFIGURATION
# ------------------------------------------------------------------------------
# The base domain for all services. Change this for production/web deployment.
# Local development: pluto.local (requires /etc/hosts entries)
# Production example: myproject.example.com
PLUTO_DOMAIN=pluto.local

# ------------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ------------------------------------------------------------------------------
# PostgreSQL credentials (used by all services that need database access)
# SECURITY: Change POSTGRES_PASSWORD before first deployment!
POSTGRES_USER=pluto
POSTGRES_PASSWORD=CHANGE_ME_BEFORE_DEPLOY
POSTGRES_DB=pluto

# ------------------------------------------------------------------------------
# ADMIN CREDENTIALS
# ------------------------------------------------------------------------------
# Shared admin credentials used across services
# SECURITY: Change these before any production use!

# n8n admin credentials
ADMIN_USER=admin
ADMIN_PASSWORD=CHANGE_ME_BEFORE_DEPLOY

# OpenWebUI admin account (created automatically on first deploy)
# The first account created becomes the administrator
ADMIN_EMAIL=admin@pluto.local
ADMIN_NAME=Admin

# ------------------------------------------------------------------------------
# SECRET KEYS (Generate unique values!)
# ------------------------------------------------------------------------------
# Generate secure keys with: openssl rand -base64 32
#
# OpenWebUI secret key (for session tokens - prevents logout on restart)
WEBUI_SECRET_KEY=GENERATE_ME_WITH_OPENSSL

# LiteLLM master key - authenticates API requests to LiteLLM
# All tools (OpenWebUI, n8n) use this key to access LiteLLM
LITELLM_MASTER_KEY=sk-GENERATE_ME_WITH_OPENSSL

# ------------------------------------------------------------------------------
# AWS CONFIGURATION
# ------------------------------------------------------------------------------
# BEST PRACTICE: Use 'aws configure' to set up credentials!
# This stores them securely in ~/.aws/credentials, NOT in this file.
#
# To set up AWS credentials:
#   1. Install AWS CLI: https://aws.amazon.com/cli/
#   2. Run: aws configure
#   3. Enter your Access Key ID, Secret Access Key, and region
#
# The containers will automatically use credentials from ~/.aws/
#
# Only the region is needed here (credentials come from ~/.aws)
AWS_REGION=us-east-1

# ------------------------------------------------------------------------------
# TIMEZONE
# ------------------------------------------------------------------------------
# Set your timezone for n8n and other services
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=America/New_York

# ------------------------------------------------------------------------------
# MANAGEMENT TOOLS CONFIGURATION
# ------------------------------------------------------------------------------
# pgAdmin4 credentials
PGADMIN_EMAIL=admin@pluto.com
PGADMIN_PASSWORD=CHANGE_ME_BEFORE_DEPLOY

