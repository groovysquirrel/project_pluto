# AWS Deployment Configuration
# Copy this file to .env and update values before running ./pluto.sh deploy aws

# The name of your project (prefix for all resources)
TF_VAR_project_name="pluto"

# The Route53 hosted zone name (must already exist in your AWS account)
# Example: patternsatscale.com
TF_VAR_hosted_zone_name="pluto.patternsatscale.com"

# Optional subdomain prefix for the root of your application
# Leave blank if you want to deploy to the root of the hosted zone
# Example: pluto (results in pluto.patternsatscale.com)
TF_VAR_subdomain_prefix=""

# Cognito Custom Domain
# This must be unique across all of AWS Cognito in the region if not using a custom domain cert (which we are not yet)
# OR it can be a subdomain of your hosted zone if we set up the matching A record (which we will try to do)
# Example: auth.pluto.patternsatscale.com
TF_VAR_cognito_custom_domain="auth.pluto.patternsatscale.com"

# Database Configuration (Optional - defaults are usually fine for dev)
# TF_VAR_db_username="pluto"
# TF_VAR_db_name="pluto"
